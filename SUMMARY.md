# 作業サマリー（2025-11-22）

- LP をルート（`/`）に昇格し、旧ルート `index.html` を削除。
- 生成フロー (`generator/*`) とアンケートページ (`form1`〜`form3`) を削除し、Vite のエントリからも除外。
- 管理画面の QR コード生成機能と `qrcode` 依存を削除。
- `_redirects` を廃止し、現在は LP を直接返す構成。

残しているページ: `/`（LP）、`/admin`（設定管理）、`/user`（店舗/ユーザー情報入力）。
